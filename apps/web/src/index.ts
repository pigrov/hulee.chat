export type WebClientBoundary = {
  runtime: "web";
  usesServerOnlyNextBehavior: true;
};

export const webClientBoundary: WebClientBoundary = {
  runtime: "web",
  usesServerOnlyNextBehavior: true
};
